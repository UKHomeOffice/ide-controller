{
  "name": "ide-controller",
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "ci": "lerna exec npm ci && lerna bootstrap",
    "lint": "lerna run lint --stream",
    "test": "lerna run test",
    "dev": "lerna bootstrap && lerna run dev --stream",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "eslint": "^7.9.0",
    "eslint-plugin-react": "^7.20.6",
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
