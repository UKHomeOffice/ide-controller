{
  "name": "ide-controller",
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "ci": "lerna exec npm ci && lerna bootstrap",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "dev": "lerna bootstrap && lerna run dev --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
